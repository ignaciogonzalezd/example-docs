/**
 * My Math library - Learn More {@tutorial second-tutorial}
 * @module MyMath
 */

/**
 * 
 * @param {number} x First umber 
 * @param {number} y Second number
 * @returns {number} Sum of x and y
 */
const add = (x, y) => x + y;

/**
 * 
 * @param {number} x First umber 
 * @param {number} y Second number
 * @returns {number} substract of x and y
 */
const substract = (x, y) => x - y;

module.exports = {
    add,
    substract
}